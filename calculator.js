const pantallaOperaciones = document.querySelector(".texto-operaciones");
const pantallaResultado = document.querySelector(".texto-resultado");
const botones = document.querySelectorAll(".panel-boton");

botones.forEach((boton) => {});

var texto_evaluar = "",
  texto_mostrar = "";

function test() {
  document.querySelector(".container").classList.toggle("dark-mode");
}
